@model IEnumerable<Funfik.Core.Entities.User>

@{
    ViewBag.Title = "All users";
}

<h2>@ViewBag.Title</h2>
<ul>
    @foreach (var user in Model)
    {
        <li>@user.Username</li>
    }
</ul>
